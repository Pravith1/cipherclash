{
  "name": "cipherclash-backend",
  "version": "1.0.0",
  "description": "CipherClash Registration Portal Backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:check": "nc -zv localhost 27017 || echo '❌ MongoDB is not running on localhost:27017'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
